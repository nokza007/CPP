#include <iostream>
using namespace std;
int main()
{
    int Sum = 0;

    Sum = Sum + 1;
    Sum = Sum + 2;
    Sum = Sum + 3;
    Sum = Sum + 4;
    Sum = Sum + 5;
    Sum = Sum + 6;
    Sum = Sum + 7;
    Sum = Sum + 8;
    Sum = Sum + 9;
    Sum = Sum + 10;

    cout << "Total of array elements : " << Sum << endl;
    return(0);
}
